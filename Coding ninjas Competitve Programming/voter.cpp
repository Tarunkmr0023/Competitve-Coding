#include bitsstdc++.h
using namespace std;
int main() {
    int n1,n2,n3;
    cinn1n2n3;
    int no;
    mapint,int m;
    for(int i=0;in1;i++)
    {
        cinno;
        m[no]++;
    }
    for(int i=0;in2;i++)
    {
        cinno;
        m[no]++;
    }
    for(int i=0;in3;i++)
    {
        cinno;
        m[no]++;
    }
    vectorint v;
    for(auto i=m.begin();i!=m.end();i++)
    {
        if(i-second1)
        {
            v.push_back(i-first);
        }
    }
    coutv.size()endl;
    for(int i=0;iv.size();i++)
    {
        coutv[i]endl;
    }
}
